# ws-ccminer
For monitoring mining devices

CCminer needs to have the api allowed on each device in a config.json

# Install

cd ws-ccminer

npm install

# start app

node server.js

1. copy the `config.json.excample` to `config.json`
2. edit `config.json` to add in the IP addresses of your miners
